name: test-1

on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]

jobs:
  test-1:
    runs-on: ubuntu-latest

    steps:
      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo "Don't mind me, just running this test workflow. With a minor change!"
